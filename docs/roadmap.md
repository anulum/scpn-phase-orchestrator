<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->
<!-- Commercial license available -->
<!-- © Concepts 1996–2026 Miroslav Šotek. All rights reserved. -->
<!-- © Code 2020–2026 Miroslav Šotek. All rights reserved. -->
<!-- ORCID: 0009-0009-3560-0851 -->
<!-- Contact: www.anulum.li | protoscience@anulum.li -->
<!-- SCPN Phase Orchestrator — Public Roadmap -->

# Public Roadmap

This roadmap is a public planning view. It avoids dates unless a release is
already published, and it separates stable surfaces from research or deferred
maintenance tracks.

## Consolidated Open-Item Register

This section is the canonical open-item list. Rows keep completed sub-items as
evidence under their parent item, then state the exact remaining scope. Detailed
sections below preserve the longer roadmap context without creating a second
queue.

| Parent item | Completed evidence consolidated here | Remaining open scope |
|-------------|--------------------------------------|----------------------|
| Public boundary validation sweep | focused constructor/config guards cover QueueWaves service config, UPDE/Stuart-Landau/Sparse/Sheaf/Torus/Inertial/Hypergraph/Simplicial/Swarmalator engines, stochastic sweeps, monitors, HDL knobs, bridges, extractors, reporting, PAC heatmaps, and audit replay boundaries; adapter dataclass boundary guards now harden Gaian mesh peer state, Remanentia coherence snapshot, Synapse agent state, and Synapse coupling snapshot constructor invariants with dedicated boundary tests; distributed phase-gossip config/message constructors now reject bool/integer alias misuse and negative wall-clock timestamps; audit stream event constructors now enforce canonical payload hash consistency and signed/unsigned signature metadata contracts; gRPC fallback request/response dataclasses now fail closed on non-positive step/stream counts, negative timing/count fields, and malformed layer payloads; runtime observability metric snapshots now enforce UPDE-state type, regime non-emptiness, non-negative finite latency, and strict step-index contracts at construction time; additional U1 guard slices now harden ActionProjector constructor and runtime previous-value boundaries (including action payload type), KnmTemplateSet registration and lookup boundaries (including non-empty descriptions, floating dtypes, trimmed lookup names, normalised tab-wrapped storage keys, and canonicalised stored template names), BoundaryObserver constructor and observe runtime boundaries (including EventBus attachment, bool/non-numeric value rejection, and bool step/bound fail-close contracts), AuditLogger constructor/header/step/event boundaries (including path-type and event-stream contracts, seed/metadata type checks, UPDEState and action-list contracts, step-index guards, `channel_runtime` payload type validation, strict `log_event` event/data payload contracts, and finite `epsilon` payload enforcement), SSGF GeometryCarrier constructor/decode/update/reset boundaries (including callback and seed contracts plus constructor/reset bool-seed and update-epsilon rejection), CyberneticClosure constructor/step/run boundaries (including boolean phase-vector and outer-step rejection), TCBO observer constructor/runtime phase-vector boundaries (including bool-alias constructor guards and boolean/non-array/non-vector/empty phase-vector rejection), PetriNetAdapter mapping/context boundaries (including non-empty mapping, normalised regime names, strict context metric name/value validation, and a fully executed fixed 36-item U1 constructor/config closure checklist), incomplete Gaian mesh peer heartbeat payload rejection, Prometheus decoded-response shape/value validation, Synapse channel decoded hub-message validation, Remanentia decoded response payload validation, Neurocore Rust backend rate-vector validation, Redis finite JSON number enforcement, audit-stream finite JSON canonicalisation, QPU artifact finite JSON metadata/handoff enforcement, audit replay finite JSONL parsing plus hash verification, audit-signing keyring finite JSON enforcement, binding-loader finite numeric config enforcement, symbolic-binding report finite JSON hash enforcement, audit-logger finite JSONL hash-chain enforcement, meta-transfer finite JSON package/replay ingestion enforcement, K_nm handshake spec finite JSON enforcement, phase-gossip wire finite JSON enforcement, Prometheus response finite JSON enforcement, Synapse hub wire finite JSON enforcement, Stuart-Landau non-negative amplitude-coupling strength enforcement, UPDE zero self-coupling diagonal enforcement, Stuart-Landau zero phase/amplitude self-coupling diagonal enforcement, stateless UPDE dispatcher zero self-coupling enforcement, K_nm template zero self-coupling enforcement, K_nm handshake zero self-coupling enforcement, Lyapunov guard/spectrum and polyglot backend zero self-coupling diagonal enforcement, order-parameter finite mean-phase enforcement, PLV empty/non-empty equal-length enforcement, layer-coherence mask bounds enforcement, finite coherence-magnitude enforcement, UPDE Rust-output phase-domain enforcement, Strang-splitting accelerated-backend phase-domain enforcement, Hodge decomposition boolean-alias rejection, plasticity boolean-alias rejection, coupling-lag physical-distance validation, K_nm geometry finite/boolean validation, spectral coupling/frequency boolean-alias rejection, E/I balance numeric-domain validation, universal-prior hyperparameter, seed, and frequency-vector validation, and coupling-inference scalar-config, phase-series, and transfer-entropy score validation, coupling-estimator boolean/complex input and harmonic least-squares fail-closed validation, and Phase-SINDy phase-series plus Rust coefficient validation, and TE-adaptive coupling input, score, and Rust-output validation, transfer-entropy complex-input, NumPy-bin, and zero-diagonal backend validation, HCP connectome scalar input and optional-backend structural matrix validation, and Bayesian UPDE posterior-fit, distribution, and config boundary validation plus delayed-UPDE boolean-alias and delay-lookup validation plus Ott-Antonsen unit-disk state and frequency-sample boundary validation plus explicit-step numerics config and CFL-boundary validation plus PAC bin-count and non-negative-amplitude enforcement plus basin-stability, bifurcation shape/finite/boolean-alias, zero self-coupling, and composite Rust output validation, Lyapunov, entropy-production public and direct-adapter shape/finite/real-valued/boolean-alias rejection, hybrid-order density-matrix positive-semidefinite validation, self-model phase-residual branch-cut wrapping, strange-loop scalar boolean-alias rejection, morphogenetic topology boolean-alias rejection, sheaf-coherence boolean-alias rejection, topos-policy condition/action contract validation, symbolic-binding topos layer-index contract validation, topos-domain finite example-hash validation, information-geometry boolean-alias rejection, information-geometry scenario boolean-alias/finite-hash validation, Poincare backend crossing-count, order-parameter, recurrence backend-matrix symmetry/diagonal, winding backend boolean-alias rejection, transfer-entropy backend boolean-alias rejection, and dimension backend boolean-alias rejection, dimension real-valued trajectory/epsilon/spectrum and backend-output validation, NPE phase, radius, score, and distance-matrix real-valued/boolean-alias rejection, ITPC phase, pause-index, output, and persistence real-valued/boolean-alias rejection, chimera phase, coupling, state, and backend-output real-valued/boolean-alias rejection, embedding signal, trajectory, scalar, and backend-output real-valued/boolean-alias rejection, and psychedelic phase, coupling, entropy-output, and polyglot-adapter real-valued/boolean-alias rejection plus PID phase, index, bin-count, and Rust scalar real-valued/boolean-alias rejection and integrated-information scalar/matrix boolean-alias rejection, physiology replay, infrastructure replay, and cyber-industrial replay boolean-alias/result validation with focused regression tests | finish the residual broad public constructor/config validation sweep from the internal U1 scope |
| Coverage debt closure | historical fixes added behaviour coverage for five modules, local coverage guard passes, focused Studio/CLI/supervisor behaviour tests cover residual service-manifest, auto-coupling CLI, formal-readiness, policy-branching, and hierarchy-ingestion paths, focused quantum/causal/meta/autotune/reporting/QPU tests cover residual adapter validation, deterministic manifests, malformed-input rejection, and artefact parsing paths, focused QueueWaves/binding/monitor tests cover residual runtime, config, digital-twin, semantic, recurrence, and ITPC fallback boolean-alias rejection paths, focused coupling/UPDE/supervisor/plugin tests cover residual spectral, Hodge, attention-residual, market, bifurcation, plugin, sheaf, alignment, predictive, Byzantine, and topology edge paths, focused visualisation/reporting/SSGF/imprint/adapter tests cover residual rendering, summary, streamer, free-energy, carrier, PGBO, imprint, Redis-schema, Modbus, QPU artefact, and hybrid co-compiler boundaries, focused CLI/formal/binding/UPDE/monitor tests cover audit-stream sidecars, watch validation, fail-closed checker manifests, binding-loader validators, derived-channel errors, backend dispatch, RK45 fallback, delayed Rust dispatch, and transfer-entropy backend routing, focused Studio/supervisor/autotune/quantum-control tests cover stable UI payloads, policy diagnostics, hierarchy/topology/predictive/Byzantine audit determinism, learner fallback alternatives, reward component ordering, quantum bridge validation, and blank cross-channel rejection, focused gRPC/audit/TE/SSGF tests cover compatibility aliases, fail-closed audit keyring parsing, optional Rust dispatch contracts, Langevin/Boltzmann/temperature Rust paths, and TE-adaptive Rust routing, and focused coupling/EVS tests cover least-squares failure fallback plus non-numeric, non-finite, boolean-alias, and result-invariant monitor rejection | reduce residual source coverage debt versus the historical CI baseline, then refresh the internal coverage inventory from current CI artefacts |
| Rust benchmark coverage | reference-suite benchmark metadata, public benchmark snapshot, and many feature gates are in place | add dedicated benchmark files for the remaining Rust modules where measured, reproducible benchmark value exists |
| CI optimisation | current CI validates the public roadmap, reference suite, security scans, and scoped feature tests | evaluate unified Python jobs, FFI coverage merge, and Hypothesis profile reduction without weakening gate quality |
| Typed NumPy signatures | visualisation helpers, STL fallback arrays, strange-loop helper arrays, symbolic extractor integer-compatible signal contracts, and UPDE public return documentation have scoped element-typed annotations; latest coupling/monitor, autotune/adapters/reporting/supervisor, and UPDE/oscillator sweeps found no residual loose source signatures | continue broader public API NDArray element-typed annotation sweeps module by module |
| JSON deserialisation hardening | `GaianMeshNode` rejects incomplete decoded heartbeat payloads before peer insertion, and `PrometheusAdapter` validates decoded response object/data/result/sample structure plus finite metric values before returning range arrays or instant scalars | continue monitoring externally supplied JSON boundaries when adjacent modules are next touched |
| One-click SPO Studio | Streamlit operator prototype covers domainpack loading, raw-source import, binding review, beginner guidance, canvas review, live metrics, replay tuning, hierarchy monitor, connector plans, runtime boundary validation, deployment/package/materialisation manifests, hardware evidence packages, recovery reports, and Docker/WASM/project exports | make Studio a finished standalone product and attach real FPGA/neuromorphic evidence |
| Auto-binding and data-driven discovery | deterministic proposal builders, sample-rate inference, sparse derivative evidence, SINDy selection, graph inference, transfer-entropy coupling export, extractor proposals, `auto_initial_k`, and benchmark-gated domain thresholds are implemented | validate on private or partner live datasets under the same review-only threshold contract |
| Guided deployment path | deployment readiness, package, materialisation, hardware target, verified evidence, connector handoff, hashes, parity status, blocked reasons, and operator commands are implemented | attach real target evidence |
| Dynamic higher-order topology adaptation | foundation, plasma-control demo, traffic-flow/network-security transfer-entropy demos, Lyapunov mutation validation, pairwise-support policy hardening, and multi-domain Lyapunov policy workflow validation are implemented | extend to additional partner domains when new reviewed domainpacks land |
| Causal intervention engine | counterfactual rollout foundation, live causal-graph learning surface, and cardiac/power-grid/traffic-flow/network-security attribution demos are implemented | add larger causal-model learners and more attribution demos |
| RL/autotune layer | reward evaluation, replay ranking, offline candidates, proposal records, replay-only/adaptive search, PPO-like/SAC-like/hybrid-physics proposal generators, differentiable Equinox supervisor objective, N-channel optimisation surface, and multi-scenario benchmark gates are implemented behind non-actuating replay gates | run real trained PPO/SAC experiments, baselines, and publication artefacts |
| FEP / predictive-coding supervisor | predictive supervisor foundation, reusable hierarchy assessment, and power-grid/cardiac hierarchy proofs are implemented | integrate deeper predictive-coding world-model support |
| Full N-channel algebra | channel algebra summary, resolved-config integration, audit/report exposure, delayed/uncertain classification, runtime policy records, supervisor execution, and replay-only channel-weight/cross-coupling optimisation are implemented | add deeper learner-backed optimisation |
| Hierarchical and distributed orchestration | reduced-summary parent orchestration, bounded escalation, edge/cloud sync envelopes, strict non-socket runtime validation, JSONL/REST/frame adapter boundaries, domainpack transport demos, offline gossip replay, and phase-vector gossip are implemented | deliver owned live transports, transport smoke gates, and broader cross-machine deployment demos |
| Digital-twin binding standard | binding contract export, transport-neutral validation, JSONL replay, in-memory adapter, manifest compatibility, REST/gRPC/Kafka adapters, no-I/O hardware boundary, and shared live/replay operator evidence are implemented | attach real hardware evidence and continue digital-twin operator productisation |
| Formal verification export | Petri-net/policy/STL exports, package manifests, checker command manifests, checker-readiness audits, and non-executing CLI package export are implemented | add deeper external model-checker CI execution and broader PRISM/SPIN/TLA+/SMT workflows for safety-critical domainpacks |
| STL runtime verification | robustness monitor, policy YAML integration, PRISM linkage, monitoring automata, non-actuating controller candidates, policy-gated projection, closed-loop plans, and reference gates are implemented | pass projected plans through the full safety/actuation runtime stack |
| Symbolic-to-binding compiler | compiler foundation, domainpack/docs retrieval evidence, confidence factors, generated review notebook, preflight evidence, retrieval diagnostics, and reference gates are implemented | expand optional corpus with larger live-doc/domainpack corpora |
| Cross-domain meta-transfer | replay-backed proposals, multi-audit fitting, nested audit corpora, package/export manifests, CLI export, entry-point evidence, pre-publish enforcement, and manifest-readiness gates are implemented | validate against larger private or partner audit-history corpora and decide optional package publication |
| Plugin ecosystem | manifest registry, entry-point discovery, capability declarations, compatibility checks, audit records, marketplace catalogue, Rust-facing registry export, deterministic metadata-only planning artefacts, `spo plugins plan-execution` output, approval-consumption `spo plugins request-execution PLAN_JSON APPROVAL_JSON` request artefacts, request-bound runtime invocation with pre-import plan/target/shape revalidation, stored-request hash validation and revocation checks, deterministic request storage manifests with retention and custody metadata, atomic local-file request bundle persistence, `spo plugins persist-execution-request` operator persistence with revocation-list input, `spo plugins revoke-execution-request` lifecycle revocation artefacts, deterministic `spo plugins revocation-list` artefacts, deterministic `spo plugins lifecycle-status` status records, deterministic `spo plugins lifecycle-summary` batch review and renewal queues, deterministic `spo plugins lifecycle-policy-report` operator dashboard action counts, deterministic `spo plugins lifecycle-renewal-queue` operator follow-up queue output, deterministic `spo plugins lifecycle-multistore-dashboard` aggregate operator dashboard output, deterministic `spo plugins lifecycle-multistore-drilldown` per-store provenance output, deterministic `spo plugins lifecycle-remediation-orchestration` priority-ordered action-plan output, deterministic `spo plugins lifecycle-remediation-action-status` state transition artefacts, deterministic `spo plugins lifecycle-remediation-execution-dashboard` closed-loop progress surfaces, deterministic `spo plugins lifecycle-remediation-deployment-handoff` deployment command handoff surfaces, deterministic `spo plugins lifecycle-remediation-scheduler-queue` scheduler-ready deployment queue output with strict window-fit validation, deterministic `spo plugins lifecycle-remediation-scheduler-telemetry` operator telemetry output, deterministic `spo plugins lifecycle-remediation-scheduler-adapter-handoff` external scheduler adapter queue handoff output, deterministic `spo plugins lifecycle-remediation-scheduler-acknowledgement` execution acknowledgement artefacts, deterministic `spo plugins lifecycle-remediation-scheduler-acknowledgement-replay` replay-manifest ingestion output, deterministic `spo plugins lifecycle-remediation-scheduler-execution-dashboard` live adapter execution aggregation output, deterministic `spo plugins lifecycle-remediation-scheduler-control-plan` interactive control action planning output, deterministic `spo plugins lifecycle-remediation-scheduler-runbook` grouped operator runbook output, deterministic `spo plugins lifecycle-remediation-scheduler-automation-profile` adapter automation profile output, deterministic `spo plugins lifecycle-remediation-scheduler-acknowledgement-capture` interactive acknowledgement capture output, deterministic `spo plugins lifecycle-remediation-scheduler-retry-profile` adapter retry/backoff policy output, deterministic `spo plugins lifecycle-remediation-scheduler-retry-orchestration` retry queue orchestration output, guarded metadata-only Rust handoff manifests, grouped dispatch records, explicit loading-disabled gates, Python-owned opt-in loading gates with manifest compatibility/package-boundary/target-callability checks, deterministic execution audit records, pre-import target-hash approval, operator identity/reference binding to `plan_hash` and `target_hash`, runtime policy envelopes that require `require_target_hash_approval` and a matching approved `target_hash` for load/execute flags, argument-value-free execution records, and deterministic non-local storage-adapter handoff manifests for object/API deployment stores are implemented | continuous operator UX refinement for adapter families remains iterative productization work |
| Observability and digital-twin polish | digital-twin operator evidence and runtime Prometheus text exports for accepted/rejected sync counts, adapter health, sequence, residual extrema, status, capabilities, directions, and mismatch reasons are implemented; deterministic `spo digital-twin-observability-bundle` links operator evidence with scheduler dashboard/replay linkage and Prometheus export text; deterministic `spo digital-twin-grafana-dashboard-pack` and `spo digital-twin-live-deployment-playbook` provide adapter-family dashboard packaging plus live rollout gate/runbook artefacts | continue adapter-family UX refinement and site-specific rollout conventions |
| Self-modelling embodied digital twin | `self_model_error` monitor, replay-backed review-only reconfiguration evidence, domain scenarios, and reference gates are implemented without live actuation | collect real hardware-in-the-loop rebinding evidence |
| Evolutionary supervisor policy search | offline replay search, STL/counterfactual filters, richer policy DSL, Petri-net/topology grammars, deterministic review gates, and disabled live merge/hot patching are implemented | add maintained operator UI surfacing |
| Information-geometry control | dependency-free Fisher-Rao/Wasserstein primitives, metric tensors, natural-gradient proposals, curvature/geodesic metrics, scenarios, and reference gates are implemented without live actuation | add optional JAX-native acceleration |
| Sheaf-cohomology control | supervisor foundation, six heterogeneous domain demos, obstruction summaries, top-residual evidence, and reference gates are implemented | validate on maintained live-domain operator datasets |
| Federated meta-orchestrator | policy-gradient aggregation, raw-data rejection, privacy accounting, hash-linked transport envelopes, secure aggregation manifests, DP noise manifests, production-boundary gates, and deployment-preflight gates are implemented | deliver actual live distributed transport, live secure aggregation execution, production DP noise service operation, and operator UI surfacing |
| Byzantine-fault-tolerant meta-orchestrator | offline three-node BFT consensus manifests verify signed policy proposals, hash-linked audit parents, quorum winners, rejected nodes, and non-actuating review gates | deliver live distributed transport |
| Quantum-native compiler target | deterministic OpenQASM 3 compiler manifests, Qiskit/PennyLane handoff, co-simulation parity, QPU-disabled readiness audits, and reference gates are implemented | execute against real QPU targets only after evidence, credentials, parity, and operator approval exist |
| Neuromorphic compiler target | deterministic Lava/PyNN schedules, population/projection/control-action records, simulator parity, hardware-write disabled audits, readiness audits, and reference gates are implemented | execute against real neuromorphic targets only after evidence and approval exist |
| Hybrid neuromorphic-quantum co-compiler | component-hash linked hybrid manifests, target-readiness audits, operator handoff packages, package hash linkage, and non-executing gates are implemented | execute real hybrid targets |
| Value-alignment supervisor guard | guard foundation, binding-spec templates, counterfactual reporting, all current domainpack templates, and replay-calibration evidence are implemented | calibrate domainpack-specific prior thresholds against real deployment or replay evidence |
| Autopoietic lineage sandbox | resource-bounded child-policy manifests, policy diffs, accepted/rejected evidence, disabled live merge/hot patch/actuation, and reference gates are implemented | add richer replay corpora and operator UI surfacing |
| Temporal-causal hypergraph experiments | research-only manifests compare time-symmetric hyperedges against lagged causal baselines with blocked evidence and reference gates | add broader causal baselines |
| Intergenerational policy inheritance | signed lineage metadata, inherited genomes, replay fitness, HMAC review metadata, reviewed-hot-patch-only semantics, and reference gates are implemented | add richer lineage histories and operator UI surfacing |
| Sheaf-theoretic coherence manifold | listed as long-horizon research track | implement obstruction-aware control primitive over a sheaf Laplacian with audit-visible cohomology dimensions |
| Constitutional value-alignment guard | listed as long-horizon research track | implement Pareto objective constraints, counterfactual violation logs, and forced safe fallback path |
| Strange-loop meta-orchestrator | strange-loop monitor foundation is implemented | add long-run drift scenarios and Studio surfacing |
| Morphogenetic field topology | field support, snapshot visualisation, SVG rendering, eight domainpack demos, and benchmark evidence are implemented | add richer Studio UI rendering |
| Integrated-information monitor | monitor foundation, audit-report summary integration, approximation benchmark cases, empirical replay corpus gates, and claim-boundary metadata are implemented | add richer Studio UI rendering |
| Topos-theoretic semantic binding | symbolic-binding functor validation, policy-composition category validation, domain obligation examples, and proof-boundary gates are implemented | add richer Studio UI surfacing and optional external categorical proof-tool export |
| Multiverse counterfactual simulator | vectorised branch rollouts, domain scenarios, fail-closed branch-risk gates, and reference evidence are implemented | add optional JAX acceleration, larger scenario corpora, and Studio surfacing |
| Entanglement-aware hybrid order parameters | dependency-free quantum co-simulation monitor, entanglement entropy, participation ratio, classical `R`/`Psi`, domain scenarios, and reference gates are implemented without QPU execution | add richer simulator backends and Studio surfacing |
| Multi-language transition | Rust/JAX remain primary; backend fallback and backend review gates are documented | CEO scope decision remains for Go orchestrator, Julia convenience, Mojo domainpacks, Lean verification, protobuf extension, and Apache Arrow distributed state sync |
| Safety/research frontier | long-horizon items are tracked but not promoted to active implementation | decide scope for certified safe control, causal discovery/control, neuromorphic deployment bridge, quantum-classical hybrid network, live digital-twin sync protocol, and federated oscillator learning |

## Current Stable Surface

| Surface | Status |
|---------|--------|
| UPDE/Kuramoto engines | Python engine family with optional Rust acceleration |
| Stuart-Landau amplitude dynamics | documented guide, API reference, examples, and tests |
| Supervisor and policy DSL | regime management, policy rules, Petri net sequencing, audit trace |
| Domainpacks | 36 bundled domainpacks with README coverage and gallery inventory |
| Notebooks and demos | 19 notebooks, 27 terminal examples, Streamlit tools, CLI demo, WASM demo |
| API documentation | package-level and detailed MkDocs API pages with public-module autodoc coverage guarded by tests |
| Deployment docs | production, backend fallback, dependency locks, interactive tools |
| Audit/replay | deterministic audit trace and replay documentation |

## Active Documentation Track

| Track | Current state | Next useful additions |
|-------|---------------|-----------------------|
| Onboarding | role-based first-hour handbook exists | keep failure paths and contributor paths linked from every first-run page |
| Troubleshooting | install, notebooks, docs, FFI, validation, audit replay, and demos covered | add issue templates if repeated failure classes emerge |
| Notebook operations | execution matrix documents extras and CI expectation | keep matrix updated when notebooks are added or made local-only |
| API examples | public modules have mkdocstrings coverage; package-level API pages exist | add compact copy/paste examples to subsystem pages that lack them |
| Domainpack docs | 36 README files exist | keep examples and gallery aligned with every new domainpack |

## Usability Moat Track

This track is the main post-docs priority for lowering the learning curve for
control engineers who do not already live in phase dynamics.

| Item | Target outcome |
|------|----------------|
| One-click SPO Studio web UI | Streamlit operator prototype now covers domainpack load, raw-source import, binding review, beginner-mode guidance with ordered walkthrough, oscillator edit review artefacts, layer/channel canvas review artefacts, canvas layout manifests, canvas topology patch artefacts, validated binding rewrite candidates, signed hash-checked binding apply with backups, deterministic Canvas interaction state for browser controls, live `R`/`Psi`/`K` metrics, replay tuning, hierarchy monitor, connector ownership plans, dry-run records, owned no-I/O runtime boundary validation for REST/gRPC/Kafka/hardware, and localhost service-process manifests with health checks, deployment readiness/package/materialisation manifests, hardware target and verified-evidence package manifests, recovery reports, and Docker/WASM/project export manifests; it is not yet a good standalone product, because real FPGA/neuromorphic evidence remains open |
| Auto-binding prototype | deterministic proposal builders now infer reviewable `binding_spec.yaml` records from time-series CSV, event-log JSON, and graph JSON with confidence factors, validation diagnostics, time-column sample-rate inference, sparse-derivative/correlation-graph/clustering evidence, lagged directed graph inference, transfer-entropy coupling audit export, extractor-parameter proposals, validator-accepted `auto_initial_k`, and benchmark-gated domain thresholds; `spo auto-bind` exports review-only binding YAML or audit JSON from local raw sources without writing files or enabling actuation; private or partner live-dataset gates remain future work |
| Guided deployment path | deployment readiness, deployment package, package materialisation, hardware target, and verified hardware evidence manifests now carry target status, required artefacts, export hashes, operator-invoked commands, blocked reasons, safety gates, hardware evidence requirements, SHA-256 checks, parity status, and connector handoff review; real target evidence remains open |
| Beginner control-engineer mode | beginner-mode guidance now explains reviewed signals, coupling knobs, objectives, supervisor regime, validation state, ordered walkthrough status, and next actions from the current replay |

## v1.0 Focus

| Focus | Acceptance shape |
|-------|------------------|
| API freeze discipline | top-level public manifest and drift test are in place; future manifest changes must carry compatibility notes |
| N-channel rollout | P/I/S remains default while additional typed channels are documented and replayable |
| Benchmark visibility | benchmark pages label commands, environment, and historical snapshot dates |
| Production hardening | deployment defaults, security scans, dependency locks, and fallback paths remain documented |
| Docs as entry point | users can move from install to run, validate, replay, and deploy without reading source first |

## v1.x Differentiator Track

| Item | Roadmap stance |
|------|----------------|
| Dynamic higher-order topology adaptation | foundation, plasma-control demo, traffic-flow/network-security transfer-entropy demos, Lyapunov mutation validation, pairwise-support policy hardening, and multi-domain Lyapunov policy workflow validation are implemented; additional partner-domain validation remains future expansion |
| Causal intervention engine | counterfactual rollout foundation, live causal-graph learning, and cardiac/power-grid/traffic-flow/network-security attribution demos are implemented; larger causal-model learners and additional attribution demos remain open |
| RL/autotune layer on JAX `nn` backend | reward evaluation, replay ranking, offline candidate generation, proposal records, replay-only policy search, adaptive replay refinement, PPO-like/SAC-like/hybrid-physics proposal generators, a differentiable Equinox supervisor objective, and deterministic multi-scenario replay learner benchmark gates are implemented behind non-actuating replay gates; real trained PPO/SAC experiments, baselines, and publication artefacts remain open |
| FEP / predictive-coding supervisor backend | predictive supervisor foundation, reusable hierarchy assessment, and power-grid/cardiac hierarchy proofs are implemented; deeper predictive-coding world-model integration remains open |
| Full N-channel algebra | channel algebra summary, resolved-config integration, audit-header coverage, report JSON/text exposure, reusable report summary payloads, delayed/uncertain classification, runtime policy records, delayed/uncertain supervisor execution with audit evidence, and replay-only channel-weight/cross-coupling optimisation surfaces are implemented; deeper learner-backed optimisation remains open |
| Hierarchical orchestration | reduced-summary parent orchestration, bounded escalation audit records, deterministic edge/cloud sync envelopes, strict transport runtime validation, decoded JSONL/REST/frame adapter boundaries, power-grid/cardiac/edge-consensus transport demos, offline gossip consensus replay, and transport-neutral phase-vector gossip for UPDE nodes are implemented; live owned transports and broader deployment remain open |
| Digital-twin binding standard | deterministic binding contract export, transport-neutral payload validation, JSONL file replay, in-memory reference adapter, adapter manifest compatibility checks, dependency-free REST/gRPC/Kafka boundary adapters, a no-I/O hardware frame boundary, and transport-neutral operator evidence are implemented for timing, layers, actuators, N-channel algebra, sync capabilities, contract hashes, directions, auth posture, topics, registered devices, safety interlocks, sequence checks, residual extrema, mismatch reasons, adapter health, and replay/live parity; real hardware evidence remains tracked separately |
| Formal supervisor verification | Petri-net and policy surfaces export to PRISM and TLA+; PRISM STL label export exists; benchmark-gated deterministic formal verification packages now bind exported artefact hashes, named safety properties, disabled checker execution, external PRISM/TLC checker command manifests, and checker-readiness records without invoking tools locally; `spo formal-export --export package --include-checker-readiness` exposes the same no-execution package manifest plus optional PRISM/TLC readiness evidence; non-executing external-checker readiness audits now report executable availability, resolved paths, ready/missing status, and disabled execution; deeper external model-checker CI execution remains open |
| STL runtime verification | robustness monitor foundation, policy YAML `stl_monitors` integration, PRISM export linkage, builtin monitoring automata synthesis, non-actuating controller-candidate synthesis, policy-gated candidate projection, offline closed-loop synthesis plans, and reference-suite gates for deterministic non-actuating plan quality are implemented; runtime integration through the full safety/actuation stack remains open |
| Symbolic-to-binding compiler | compiler foundation, domainpack/docs retrieval evidence, confidence factors, generated review notebook, notebook preflight execution evidence, deterministic retrieval ranking diagnostics, and reference-suite retrieval-ranking gates are implemented; optional corpus expansion with larger live-doc/domainpack corpora remains open |
| LLM-guided domainpack scaffolding | `spo scaffold --llm` now turns natural-language intent into a strict JSON proposal, deterministic binding YAML, validator-approved domainpack files, and an audit record; live providers must be explicitly configured or an offline proposal file must be supplied |
| Cross-domain meta-transfer | replay-backed proposals, multi-audit fitting, nested audit-directory corpus loading, benchmark-gated nested corpus proposal quality, training summaries, deterministic JSON package export, `scpn-meta` packaging-readiness manifests, CLI manifest export, package entry-point metadata, package entry-point release evidence, direct console-entry invocation evidence, non-publishing distribution evidence, pre-publish enforcement, and reference-suite manifest-readiness gates are implemented; larger private or partner real audit-history corpora and actual optional package publication remain open |
| Plugin ecosystem | manifest registry foundation is implemented for entry-point discovery, capability declarations, compatibility checks, audit records, marketplace catalogue packaging, runnable examples, CLI catalogue export, Rust-facing flattened registry export, benchmark-gated guarded metadata-only Rust runtime handoff manifests/CLI export with grouped dispatch records, target hashes, blocked incompatible entries, mutually exclusive Rust output modes, deterministic hashes, loading disabled by default, Python-owned opt-in runtime loading for approved callable capability targets, opt-in runtime execution with deterministic execution audits, pre-import target-hash approval for production deployment gates, a non-loading planning artefact layer with immutable invocation-shape records (argument values withheld), `spo plugins plan-execution` operator JSON output, request-consumption `spo plugins request-execution PLAN_JSON APPROVAL_JSON` artefacts that bind operator identity/reference to `plan_hash`/`target_hash` plus reviewed capability matching, request-bound runtime invocation that rejects stale argument shapes before import, request hash revocation for approval rotation, deterministic storage manifests for approved requests, atomic local-file bundle persistence, CLI persistence, CLI revocation artefacts, `spo plugins revocation-list`, revocation-list-aware persistence, deterministic non-local storage-adapter handoff manifests, `spo plugins lifecycle-status`, `spo plugins lifecycle-summary`, `spo plugins lifecycle-policy-report`, `spo plugins lifecycle-renewal-queue`, `spo plugins lifecycle-multistore-dashboard`, `spo plugins lifecycle-multistore-drilldown`, `spo plugins lifecycle-remediation-orchestration`, `spo plugins lifecycle-remediation-action-status`, `spo plugins lifecycle-remediation-execution-dashboard`, `spo plugins lifecycle-remediation-deployment-handoff`, `spo plugins lifecycle-remediation-scheduler-queue`, `spo plugins lifecycle-remediation-scheduler-telemetry`, `spo plugins lifecycle-remediation-scheduler-adapter-handoff`, `spo plugins lifecycle-remediation-scheduler-acknowledgement`, `spo plugins lifecycle-remediation-scheduler-acknowledgement-replay`, `spo plugins lifecycle-remediation-scheduler-execution-dashboard`, `spo plugins lifecycle-remediation-scheduler-control-plan`, `spo plugins lifecycle-remediation-scheduler-runbook`, `spo plugins lifecycle-remediation-scheduler-automation-profile`, `spo plugins lifecycle-remediation-scheduler-acknowledgement-capture`, `spo plugins lifecycle-remediation-scheduler-retry-profile`, and `spo plugins lifecycle-remediation-scheduler-retry-orchestration`; continuous operator UX refinement for adapter families remains iterative productization work |

## Next High-Impact Differentiated Moves

These items are the next product/research moves that would most clearly
separate SPO from ordinary simulation or dashboard tooling. They are TODO
items until they have implementation evidence, tests, and reproducible docs.

| Move | Acceptance shape |
|------|------------------|
| Auto-discovery / data-driven binding | raw CSV, sensor logs, and event streams produce reviewable oscillator, channel, initial `K`, and binding proposals via SINDy, coupling estimation, clustering, and validation diagnostics; zero-config CSV sample-rate inference, sparse-derivative evidence, phase-aware Kuramoto SINDy evidence, residual-scored SINDy library selection, correlation graph evidence, lagged directed graph inference, transfer-entropy coupling audit export, extractor-parameter proposals, validator-accepted `auto_initial_k` binding, and reference-suite proposal-quality benchmarks are in place; deterministic domain-like fixtures now enforce domain-specific gates for extractor coverage, expected-edge recall, validation errors, sample count, and proposed-edge multiplier before any automatic runtime use; private or partner live datasets remain future work under the same review-only threshold contract |
| Bayesian UPDE uncertainty | sampled `omega` and `K_nm` distributions propagate through the existing UPDE kernel and report posterior-predictive `R ± sigma`, credible intervals, sampled final phases, and JSON-safe audit diagnostics; Gaussian posterior fitting from observed Kuramoto trajectories is benchmark-gated for residual quality, parameter recovery, uncertainty width, zero-diagonal non-negative coupling, and posterior rollout sample count; reserved NumPyro/BlackJAX sampler names are benchmarked as fail-closed until real validated sampler implementations exist |
| RL / optimisation on knobs | JAX `nn/` and `autotune` train replay-only or simulator-backed policies for `K`, `alpha`, `zeta`, `Psi`, channel weights, and cross-channel gains using `R_good - penalty(R_bad, safety, regime churn)` objectives; `nn.supervisor` now provides a differentiable closed-loop policy/loss surface, and the reference suite now gates PPO-like, SAC-like, and hybrid-physics replay proposals across deterministic multi-scenario fixtures while full PPO/SAC experiments, baselines, and publication artefacts remain open |
| Hierarchical and distributed orchestration | nested edge supervisors maintain local coherence, exchange bounded aggregate evidence upward, and synchronise UPDE phase vectors through digest-checked, sequence-checked, bounded-correction gossip; live owned transports and transport smoke gates now sit on replay and adapter semantics proof for production ownership |
| Formal verification export | Petri nets, policy DSL, STL monitors, and supervisor transitions export to PRISM, SPIN, TLA+, SMT, or equivalent proof workflows with reproducible safety artefacts for plasma, power, and medical-style domainpacks |
| Neuromorphic and quantum tighter integration | validated phase-control plans emit Lava/BrainScaleS-style schedules and QPU control schedules through partner bridges, with execution disabled until real target evidence, hashes, parity, and operator approval exist |
| Plugin ecosystem | standard Python and Rust extension interfaces let domain experts add extractors, monitors, actuators, and bridges with schema validation, capability metadata, audit records, compatibility tests, operator-signed approval artefacts that bind operator identity/reference to `plan_hash` and `target_hash`, and no core fork |
| Observability and digital-twin polish | Prometheus/Grafana live-run telemetry, digital-twin residual channels, adapter health, replay linkage, and twin mismatch evidence become first-class operator surfaces; digital-twin sync validations now reduce to a shared live/replay operator evidence record, runtime Prometheus text exports accepted/rejected sync counts, adapter health, latest sequence, residual extrema, status, capabilities, directions, and mismatch reasons from the same record, `spo digital-twin-observability-bundle` provides deterministic evidence-plus-replay linkage packaging, and `spo digital-twin-grafana-dashboard-pack`/`spo digital-twin-live-deployment-playbook` provide deterministic dashboard+rollout operationalisation |

## Deferred Maintenance Track

These items are acknowledged but not the current documentation slice:

| Item | Status |
|------|--------|
| Typed NumPy signature sweep | tracked as maintenance; STL fallback robustness and strange-loop supervisor helper arrays are parameterised; continue scoped module-by-module sweeps |
| Visual and batch-heavy features | deferred unless a user-facing workflow needs them immediately |
| Broad benchmark-file expansion | useful, but benchmark numbers must be measured and reproducible before publication |
| Thin-test strengthening | long-running maintenance backlog; do opportunistically when touching a module, but do not let broad test-hardening displace active feature/module roadmap work |
| Superficial assertion audit | initial audit is recorded in the local internal ledger; remediate flagged candidates incrementally as modules are changed by replacing shape/type/existence-only assertions with behavioural, invariant, error-semantic, parity, or pipeline-effect checks |
| Auxiliary backend review | keep non-primary backends experimental unless measured value justifies maintenance cost |

## Research And Experimental Track

These areas may evolve behind explicit experimental flags or separate guides:

| Area | Roadmap stance |
|------|----------------|
| ML-assisted binding proposals | promising, but outputs must remain reviewable binding specs |
| Learned supervisor policies | differentiable `nn.supervisor` policy surface exists for replay/simulator training; live use remains research-only until auditability, safety constraints, and benchmark evidence are complete |
| Distributed edge orchestration | useful for multi-node deployments; keep audit and replay semantics central |
| Formal verification exports | desirable for safety-critical policies, but separate from normal user onboarding |
| Neuromorphic and quantum-native backends | bridge work remains experimental unless a maintained workload depends on it |

## Long-Horizon Research Backlog

These are candidate research tracks. Each needs a proof-of-concept, safety
boundary, audit format, and reproducible domainpack demo before it can move
into the active roadmap.

| Track | First acceptance gate |
|-------|-----------------------|
| Self-modelling embodied digital twin | dependency-free `self_model_error` monitor, replay-backed review-only reconfiguration evidence across power-grid, cardiac-rhythm, traffic-flow, and cyber-industrial scenarios, and reference-suite gates are implemented without live actuation; real hardware-in-the-loop rebinding remains open |
| Evolutionary supervisor policy search | deterministic offline search now mutates policy genomes over audit-style replay summaries, STL robustness, replay violation, and counterfactual safety-margin filters with operator-review-only outputs; richer policy DSL, Petri-net, and topology mutation grammars are implemented with deterministic review-only reference gates; maintained operator UI surfacing remains open |
| Information-geometry control layer | dependency-free NumPy/JAX-compatible Fisher-Rao and Wasserstein review primitive now emits audit-visible metric tensors, natural-gradient proposals, curvature/geodesic metrics, deterministic domain scenarios, and reference-suite gates without live actuation; optional JAX-native acceleration remains open |
| Sheaf-cohomology control | supervisor foundation; edge-consensus, power-grid, network-security, cardiac-rhythm, manufacturing-SPC, and traffic-flow heterogeneous-domain demos; obstruction-summary triage; deterministic top-residual evidence; and reference-suite gates are implemented; additional live-domain validation remains open |
| Federated meta-orchestrator | deterministic review-only federated policy-gradient aggregation now validates clipped node updates, rejects raw time-series fields, accounts privacy budgets, emits non-actuating audit evidence, and has reference-suite gates; offline hash-linked transport envelopes, secure aggregation manifests, DP noise-service manifests, combined production-boundary gates, and deployment-preflight gates for owned transport, secure aggregation custody/quorum, and DP noise-service readiness are implemented; actual live distributed transport, live secure aggregation execution, and production DP noise service operation remain open |
| Byzantine-fault-tolerant meta-orchestrator | offline three-node BFT consensus manifests now verify signed policy proposals, hash-linked audit parents, quorum winners, rejected nodes, and non-actuating review gates; live distributed transport remains open |
| Quantum-native compiler target | deterministic OpenQASM 3 compiler manifests now cover Qiskit/PennyLane handoff with Z-frequency terms, symmetrised XY coupling terms, co-simulation parity evidence, SHA-256 hashes, QPU execution disabled, non-executing target-readiness audits for backend, credential, operator-approval, and stable-hash evidence, and reference-suite gates for blocked/ready target-readiness quality; real QPU target execution remains open |
| Neuromorphic compiler target | deterministic Lava/PyNN schedule manifests from `UPDEState` now include population records, projections, control-action review records, simulator parity evidence, SHA-256 coverage, disabled hardware writes, non-executing hardware target-readiness audits, and reference-suite gates for blocked/ready target-readiness quality; real neuromorphic target execution remains open |
| Hybrid neuromorphic-quantum co-compiler | deterministic hybrid manifests now combine quantum compiler and neuromorphic schedule artefacts under shared N-channel audit semantics, component hashes, parity status, disabled execution gates, non-executing hybrid target-readiness audits that link quantum and neuromorphic readiness hashes before operator handoff, deterministic non-executing operator handoff packages, and reference-suite gates for hash-linked ready/blocked readiness and package evidence; real hybrid target execution remains open |
| Value-alignment supervisor guard | guard foundation, binding-spec templates, counterfactual violation/score reporting, all current domainpack templates, and deterministic replay-calibration evidence are implemented, including cardiac rhythm, power grid, autonomous vehicle, satellite, power-safety N-channel, network-security, financial-markets, chemical-reactor, manufacturing SPC, robotic CPG, swarm-robotics, traffic-flow, plasma-control, fusion-equilibrium, neuroscience EEG, brain-connectome, sleep-architecture, circadian-biology, epidemic SIR, agent-coordination, quantum-simulation, identity-coherence, PLL-clock, digital-twin N-channel, edge-consensus N-channel, firefly-swarm, QueueWaves, rotating-machinery, laser-array, bio-stub, vortex-shedding, musical-acoustics, gene-oscillator, geometry-walk, metaphysics-demo, and minimal-domain; real deployment calibration remains open |
| Autopoietic lineage sandbox | deterministic resource-bounded offline child-policy lineage manifests over audit replay summaries are implemented with review-only policy diffs, accepted/rejected child evidence, disabled live merge/hot patch/actuation flags, and reference-suite gates; richer replay corpora and operator UI surfacing remain open |
| Temporal-causal hypergraph experiments | deterministic research-only manifests now compare time-symmetric hyperedge candidates against conventional lagged causal baselines with baseline-beaten evidence, blocked non-winning candidates, disabled production claims, and reference-suite gates; broader causal baselines remain open |
| Intergenerational policy inheritance | signed lineage metadata, inherited policy genomes, multi-objective replay fitness, and merge-only reviewed hot-patch manifests are implemented with deterministic benchmark gates; richer lineage histories and operator UI surfacing remain open |
| Sheaf-theoretic coherence manifold | obstruction-aware control primitive over a sheaf Laplacian with audit-visible cohomology dimensions |
| Constitutional value-alignment guard | Pareto objective constraints in binding specs, counterfactual violation logs, and forced safe fallback path |
| Strange-loop meta-orchestrator | monitor foundation is implemented; long-run drift scenarios and studio surfacing remain open |
| Morphogenetic field topology | field foundation, dependency-free snapshot visualisation, passive SVG rendering, plus swarm-robotics split-flock, power-grid stressed-replay, traffic-flow corridor-spillback, plasma-control edge-localised, network-security lateral-movement, chemical-reactor thermal-stability, manufacturing-SPC tool-wear, and robotic-CPG gait demos are implemented with reference-suite gates; richer Studio UI rendering remains open |
| Integrated-information monitor | monitor foundation, audit-report summary integration, expanded deterministic approximation benchmark cases, and physiology/infrastructure/cyber-industrial empirical replay corpus gates are implemented; richer Studio UI rendering remains open |
| Topos-theoretic semantic binding | symbolic-binding functor validation, policy-composition category validation, deterministic domain obligation examples, and reference-suite proof-boundary gates are implemented; richer Studio UI surfacing and optional external categorical proof-tool export remain open |
| Multiverse counterfactual simulator | deterministic vectorised branch rollouts, domain scenario fixtures, and fail-closed non-actuating branch-risk gates are implemented with reference-suite evidence; optional JAX acceleration, larger scenario corpora, and Studio surfacing remain open |
| Entanglement-aware hybrid order parameters | dependency-free quantum co-simulation monitor and scenario fixtures now reject boolean aliases at phase, state, qubit-count, metric, and bipartition boundaries and report bipartition Von Neumann entanglement entropy, normalised entropy, participation ratio, classical `R`/`Psi`, deterministic domain scenarios, and reference-suite gates without QPU execution; richer simulator backends and Studio surfacing remain open |

Priority order for the first implementation tranche:

1. ~~Causal intervention engine foundation.~~
2. ~~STL runtime verification foundation.~~
3. ~~Dynamic higher-order topology adaptation foundation.~~
4. ~~FEP / predictive-coding supervisor backend foundation.~~
5. ~~Symbolic-to-binding compiler foundation.~~

Speculative priority order after the first tranche:

1. ~~Strange-loop meta-orchestrator foundation.~~
2. ~~Morphogenetic field topology foundation.~~
3. ~~Integrated-information monitor foundation.~~
4. ~~Sheaf-theoretic coherence manifold foundation.~~
5. ~~Constitutional value-alignment guard foundation.~~

## Minor Polish Before v1.0

| Item | Required before claiming done |
|------|-------------------------------|
| Public benchmark suite | reference-suite metadata, dated JSON, and public snapshot page are in place; broader cross-host/backend comparison coverage remains open |
| Windows Rust FFI stability | remove the experimental warning only after CI, installation, and parity evidence support it |
| End-to-end hardware example | at least one real FPGA or neuromorphic output path with documented command, artefact, and verification result |

## Non-Goals For The Current Docs Track

- Do not present historical benchmark snapshots as current measurements.
- Do not promise release dates for research features.
- Do not hide optional dependency fallback paths.
- Do not add public APIs without corresponding docs and examples.
- Do not make users infer commands from source code when a guide can state them.

## Source Of Truth

Use this page for public orientation. Use `ROADMAP.md` in the repository root
for the detailed historical release plan and internal planning notes.
