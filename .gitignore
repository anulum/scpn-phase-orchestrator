# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.eggs/
dist/
build/
*.whl

# Virtual environments
.venv/
venv/
.env

# Tools
.mypy_cache/
.ruff_cache/
.pytest_cache/
.hypothesis/
.coverage
.coverage.*
coverage*.xml
htmlcov/
.ipynb_checkpoints/
.tox/

# Benchmark artifacts (keep baseline.json tracked)
bench/*.json
!bench/baseline.json

# MkDocs
site/

# Rust
target/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Internal business docs — never commit
docs/internal/
experiments/.eeg_cache/

# Coordination — local-only session logs and handovers
.coordination/

# Claude Code local config — never commit
CLAUDE.md
.claude/

# Local temporary files
.audit_tools/
.tmp_pytest_ok/
.venv-linux/
HANDOVER_*.md

# WASM binaries (built artifacts, not source)
*.wasm

# Local backups — never committed
BACKUP/
ARCHIVE/

# Agent metadata dropped by cross-agent tooling — tracked locally,
# never committed to this repo
.agent_metadata.json

# AttnRes compiled artefacts — rebuilt on demand from their source
# (directories may not exist until the Julia / Go / Mojo multi-head
# ports land; kept here for when they do).
go/libattnres.so
go/libattnres.h
mojo/attnres_mojo
mojo/libattnres_mojo.so

# order_params compiled artefacts
go/liborder_params.so
go/liborder_params.h
mojo/order_params_mojo

# pac compiled artefacts
go/libpac.so
go/libpac.h
mojo/pac_mojo
mojo/order_params_mojo
